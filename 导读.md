# 部署代码导读

这是线上跑的 Cloudflare Worker，代码一字未改（配置里的账号 ID、KV id、域名换成了占位符）。

```
wrangler.toml     部署配置，Workers AI 绑定就声明在这里
src/index.js      Worker 全部源码，2900+ 行，234 KB
```

每个客户一个独立 CF 账号、一个独立 Worker，跑的都是这同一份代码。
现在 49 个号里有 18 个的 glm-5.2 调不动（429 / 错误码 4006），同号 kimi、llama 正常。

---

## 想直接看 AI 调用的话，看这几行

**模型常量**

- `src/index.js:18` —— `"glm-5.2": "@cf/zai-org/glm-5.2"`
- `src/index.js:23` —— `DEFAULT_MODEL = "@cf/zai-org/glm-5.2"`
- `src/index.js:1149` —— `resolveModel()`，把各种别名归一到完整模型 ID

**绑定声明**

- `wrangler.toml` 里的 `[ai] binding = "AI"` —— 代码中的 `env.AI` 由它而来

**五个真正调用上游的地方**（全部是 `env.AI.run`，没有一处走 REST）

| 行号 | 场景 |
|---|---|
| `src/index.js:1036` | 健康探针（最小请求，`max_tokens=4`）—— 这个也照样 4006 |
| `src/index.js:1481` | 非流式，带超时 |
| `src/index.js:2208` | 流式，连接阶段带 GRACE 竞速 |
| `src/index.js:2280` | 流式，主路径 |
| `src/index.js:2371` | 流式，早回 200 之后的透明重连 |

**第三参 options**

- `src/index.js:1502` —— 只在有会话亲和时才传 `{ extraHeaders: { "x-session-affinity": ... } }`，
  用于前缀缓存。没有亲和就传 `undefined`。除此之外没传任何非常规参数。

**错误处理相关**

- `src/index.js:1813` —— `friendlyError()`，把上游错误翻译成给客户看的话
- `src/index.js:1221` —— `capState()`，容量类错误的重试策略

---

## 几点说明

- **代码本身应该没问题** —— 同一份代码在 31 个号上跑得好好的，只有 18 个号不行。
  所以怀疑不是代码，是账号侧的东西。但也可能是哪里写得不对，麻烦帮着看一眼。
- **`env.AI.run` 就是绑定方式**，Worker 和 Workers AI 在同一个 CF 内部直接调用。
  也试过用 REST 直接打官方接口绕开 Worker，同一个坏号照样 4006 ——
  所以两条路共用同一份配额。
- **第 30~31 行那两个 base64 长串**是给客户用的一键配置脚本，跟 AI 调用无关，可以跳过。

详细的排查过程和已经排除掉的原因，在另一份《给朋友-glm52断供分析材料.md》里。
